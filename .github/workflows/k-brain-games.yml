name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: npm install, start, and lint
      run: |
        make install
        make start
        make lint
        env:
          CI: true
